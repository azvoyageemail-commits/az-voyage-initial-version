{
  "name": "agency-rpfx",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:frontend": "npm run dev --prefix agency-travel",
    "dev:cms": "npm run dev --prefix cms",
    "build": "npm run build --prefix agency-travel && npm run build --prefix cms",
    "build:frontend": "npm run build --prefix agency-travel",
    "build:cms": "npm run build --prefix cms",
    "seed": "npm run seed --prefix cms"
  }
}
